= render :partial => "/shared/header", :locals => { :title => "Edit User", :subtitle => @user.permalink, :flash => flash }

- form_for @user do |f|
  = f.error_messages
  = f.label :permalink
  = f.text_field :permalink
  
  = f.submit "Update User"
  