= render :partial => "/shared/header", :locals => { :title => @bucket.permalink, :subtitle => "", :flash => flash }

.button_row
  = link_to "Edit Bucket", edit_bucket_path(@bucket), :class => "button"
